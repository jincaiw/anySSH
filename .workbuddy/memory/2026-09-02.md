# 2026-09-02

## v0.12.0 发布（jincaiw/anySCP）

- 首次 Release run（33572707854）4 个构建 job 全挂，诊断出三个独立根因并修复（commit `91bb9e9`）：
  1. macOS ×2：GitHub 缺失 secret 渲染为空字符串但变量"存在"，Tauri 把空 `APPLE_CERTIFICATE` 当已配置签名 → 钥匙串导入空证书失败。修复：新增 "Configure optional signing environment" 步骤，仅当 secret 非空才写入 `$GITHUB_ENV`。
  2. Windows：`package-portable.mjs` 用 `new URL().pathname` 取根目录，Windows 得到 `D:\D:\...`。修复：`fileURLToPath()`。
  3. Linux：打包实际成功，挂在上传步骤——`ls *.zip *.tar.gz` 中 zip glob 不匹配 → ls exit 2 → pipefail 下整步静默失败。
- 第二次 run（33574395110）：macOS 签名修复生效（tauri-action ✓、打包 ✓），但上传又挂——macOS 交叉编译产物在 `target/<triple>/portable/`，比 `target*/portable` glob 深一层。修复：改用 `find -path "*/portable/*"`（commit `bab44b9`），find 无匹配也 exit 0，天然 pipefail 安全。
- 教训：glob 修 CI 路径问题时先看产物实际层级；`--target` 交叉编译会多一层 triple 目录。
- 第 3 轮 run（33575597419）**全绿**：Windows 新根因是 Git for Windows 的 GNU tar 优先于系统 bsdtar，`tar -a -cf D:\...` 把 `D:` 当远程主机（"Cannot connect to D: resolve failed"）且 GNU tar -a 不支持 zip。修复（commit `c9817c6`）：显式调用 `%SystemRoot%\System32\tar.exe`（Windows 自带 bsdtar），缺失时回退 PowerShell `Compress-Archive`。
- v0.12.0 已正式发布：https://github.com/jincaiw/anySCP/releases/tag/v0.12.0 ，13 个资产（2×dmg、deb/AppImage/rpm、msi/setup.exe、2×app.tar.gz、4×portable 包）。
- 流程：修 workflow 必须移 tag 重推（Actions rerun 用原始 SHA 的 workflow 文件）；等当前 run 完再移 tag，避免并发上传同一草稿 Release。

## 沙箱工具笔记

- PyYAML 不在系统/托管 python 里，需 `pip install pyyaml` 到 managed venv。
- `gh api <job-logs>` 在 run 未结束时可拉单个已完成 job 的日志（`gh run view --log-failed` 不行）；输出含 ANSI 转义需 `--allow-escape-sequences`。

## 关于与更新页中文化 + 仓库地址切换（commit `2b1f2ff`）

- AboutSettings/AboutCard/UpdateChecker 三个组件硬编码英文，未接 i18n；`settings.about.*` 和 `updater.checker.*` 键早已存在于双语包 → 补 `useTranslation` + 替换文案即完成。
- 仓库地址 macnev2013 → jincaiw：SettingsPage REPO_URL、UpdateDialog REPO_URL、tauri.conf.json updater endpoint、Cargo.toml repository/homepage。
- **不可改**：tauri.conf.json `identifier: com.macnev2013.anyscp` 与 telemetry.rs 的同名目录——决定应用数据存储路径，改了会丢老用户数据。
- 验证：tsc ✅、vitest 133/133（i18n parity 8/8）。en-US 文案未变（E2E 安全），仅组件改用语言包。

## 发布 v0.14.0

- 确认本会话在 main 上的算法修复与已推送的 `a1f20f4`（另一会话，含 NoCommonAlgo 诊断）重复，备份后丢弃本地改动（`git diff > .workbuddy/backups/ssh-algorithm-fix-local-2026-09-02.patch`）。
- 本地 main 快进至 jincaiw/main（`a1f20f4`，含 i18n/portable 等全部未发布内容）；merge 时注意 .workbuddy/memory 文件冲突（仓库已 track memory 目录）。
- 后端 cargo check + ssh 测试 22/22 ✅；pnpm 在沙箱内 symlink 受限无法 install/typecheck，改以 jincaiw/main CI 全绿（run 33597952385）作为验证依据。
- 发布方式：打 annotated tag `v0.14.0` 推送至 jincaiw 远端，触发 release.yml（v0.13.0 耗时约 20 分钟，产物为 draft release 待手动发布）。

- v0.14.0 Release 构建成功（run 33599749427，11 个产物齐全），已 `gh release edit --draft=false --latest` 转正式发布并附 Release Notes：https://github.com/jincaiw/anySCP/releases/tag/v0.14.0 。

- 发现：jincaiw 仓库未配置 `TAURI_SIGNING_PRIVATE_KEY` secret → release.yml 走 `createUpdaterArtifacts=false` 分支，v0.13.0/v0.14.0 均无 latest.json/签名产物，应用内更新器会 404。需用户在仓库 Secrets 配置 minisign 私钥后，后续版本才能启用应用内更新。

## 修复 "Authentication failed: server rejected credentials"（commit 94eed4d）

- 根因一：russh `authenticate_password` 只发 `password` 方法，网络设备/加固 sshd 常只允许 `keyboard-interactive`。
- 根因二：russh-keys 对 RSA 密钥固定用 rsa-sha2-512 签名（`decode_openssh` → `SHA2_512`），老设备只认 rsa-sha2-256 甚至 `ssh-rsa`(SHA-1)。
- 修复（manager.rs）：密码认证失败后回退 keyboard-interactive（所有 prompt 用密码作答，上限 8 轮）；RSA 公钥认证失败后按 SHA2_256 → SHA1 重签重试（`KeyPair::with_signature_hash`）。终端/SFTP/SCP/健康检查/ProxyJump 全部经 `authenticate_handle` 生效。
- 验证：cargo test 214/214、clippy 干净；已推送 jincaiw/main（CI 触发）。注意 git 提交者身份是自动生成的 jason.wa@jincaideMac-mini.local。

## 发布 v0.14.1（认证回退修复）

- CI 通过（run 33602849233）后打 tag v0.14.1 → Release 构建成功（run 33603654642，11 产物齐全）→ 直接 `--draft=false --latest` 发布并附 Notes：https://github.com/jincaiw/anySCP/releases/tag/v0.14.1 。
- 标准发布流程已固化：push commit → watch CI → tag vX.Y.Z → watch release.yml（约 17 分钟）→ 核对产物 → publish draft + latest + Notes。

## 发布 v0.14.2（堡垒机认证）

- 用户确认场景：堡垒机先问用户名、再弹密码框（多 prompt KI）；v0.14.1 全部 prompt 填密码 → 用户名字段错填密码被拒。
- 两次提交：`cbd7cd0` KI 优先 + 英文 prompt 智能作答 + 30s 超时（期间 CI 因 cargo fmt 失败一次，已 amend）；追加中文 prompt 识别（密码/口令/验证码/令牌先判，用户/账号/帐号/账户/登录名次之，混合词"用户密码"归密码）。
- v0.14.2 Release 构建成功（run 33609109338，11 产物），已直接发布 Latest：https://github.com/jincaiw/anySCP/releases/tag/v0.14.2 。
- 教训：提交前必须本地 `cargo fmt`（CI 有 fmt 检查）；russh 0.46 KI reply 循环在连接断开时无 None 出口，会对 await 挂起——外层必须加 timeout。

## v0.14.3 — vault 静默失败修复 + 认证诊断（commit 1a81162）
- 排查 v0.14.2 堡垒机仍报 "server rejected credentials"：portable vault（vault/portable.rs, AES-256-GCM + Argon2id）测试覆盖完整、无问题；真凶是 commands.rs `resolve_auth_method` 把任何 vault 读错误（NotFound/损坏/key 不匹配）静默吞成空密码发往服务器。
- 修复：密码认证严格化 —— NotFound → 提示重新保存密码；凭据类型不符 / vault 损坏 / 读失败 → 各自独立报错；空密码同样直接报错。私钥 passphrase 查询保持 best-effort。
- 认证失败错误信息附带尝试轨迹：尝试过的方法、收到的 KI 提示文本（截断 40 字符）、是否发送空密码 —— 现场无 ssh -v 也能从截图定位。新增 AuthOutcome 结构（模块级，不能放 impl 块内）。
- 流程：cargo fmt（cargo fmt 会改文件导致 Edit 工具冲突，改前需重读）→ cargo test --lib ssh::（23 通过）→ clippy 干净 → push jincaiw。
- 教训：Clippy/编译报 non-exhaustive match 时，注意 StoredCredential 两个变体（Password/KeyPassphrase）在两个分支中都要覆盖。

## v0.14.4 — updater 签名密钥轮换（commit 7d6e3eb）
- 根因：tauri.conf.json 内嵌的 updater 公钥（ID 11265DE26F4D5CB5）来自上游 macnev2013，私钥从未存在于 jincaiw 仓库/本地，应用内更新器从不可用（latest.json 404）。
- 修复：`npx @tauri-apps/cli@2 signer generate -w ~/.tauri/anyssh-updater.key --password ""` 生成新密钥对（公钥 ID 94AB325C3DD57B3）；`gh secret set` 设置 TAURI_SIGNING_PRIVATE_KEY / _PASSWORD（空密码须用 `printf "" | gh secret set` 管道，--body "" 会进交互模式挂起）；替换 tauri.conf.json 公钥；tag v0.14.4 触发构建。
- 注意：~/.tauri/anyssh-updater.key 是签名私钥，绝不能提交进仓库；丢失后旧客户端将无法再验证更新。旧版（≤v0.14.3）内嵌旧公钥，更新器仍不可用属预期。
- 版本号由 tag 注入（release.yml "Set version from tag"），无需手改 package.json / tauri.conf.json / Cargo.toml。
- v0.14.4 已发布为 Latest：25 个资产含 latest.json + 全部 .sig，latest.json 全平台签名 URL 指向 releases/latest/download/。≤v0.14.3 内嵌旧公钥，其应用内更新器不可用属预期，需手动安装 v0.14.4 一次。
- 端到端验证：更新 endpoint https://github.com/jincaiw/anySCP/releases/latest/download/latest.json 公开可访问（HTTP 200，v0.14.4，11 平台）；minisign 签名 key ID（B357DD3D5C32AB94，小端显示）与内嵌公钥逐一比对全部一致，「公钥↔私钥↔签名」链路自洽。注意 minisign 规范：公钥算法为 "Ed"，签名为 "ED"，属正常差异。

## v0.14.5 — none 探测 + 服务器方法列表暴露（commit f79a50d）
- v0.14.4 诊断轨迹证实：堡垒机对 420102-7 直接拒绝 KI（无 prompts）和 password，密码非空 —— vault 链路正常，问题在协议层。
- AskUserQuestion 关键事实：其他工具是 PuTTY/Xshell/SecureCRT，420102-7 填在 SSH 用户名栏，anySSH 弹框密码是连接时输入的。
- 根因假设：OpenSSH/PuTTY 认证前先发 `none` 请求探测，russh 直接发 KI/password；自研堡垒机状态机依赖该探测。
- 实现：vendor russh 0.46（src-tauri/vendor/russh + [patch.crates-io]），补丁点 5 处 —— Reply::AuthFailure 增加 remaining_methods 字段、Handle 新增 last_auth_methods 暂存 + take_last_auth_methods()、encrypted.rs 解析构造、mod.rs 三处匹配更新。
- manager.rs：认证序列改为 none 探测（ harvest 服务器方法列表）→ 按列表门控 KI/password（未提供则跳过）→ 密码指纹（chars 数 + ascii，不含密码本身）→ 错误轨迹显示 "server allows [...]"。
- 坑：内层 match 分支类型不一致（Response vs Option）编译错，重构为先 bind timeout 再 match。
- v0.14.5 已发布为 Latest：25 资产含 latest.json。等用户实测：none 探测可能直接修复；若仍失败，错误信息将含服务器方法列表 + 密码指纹，可一次定位。

## Release 标题规范化

- v0.14.3/0.14.4/0.14.5 发布时标题用了「v0.14.x — 描述」格式，与历史的「anySSH vX.Y.Z」不一致，已用 `gh release edit --title` 统一改为 `anySSH v0.14.5` 等简洁格式。规范已写入 anyssh-release-flow skill（步骤 7）：标题固定 `anySSH vX.Y.Z`，描述写 notes。

## v0.14.6 — 仓库更名 anySSH + 全新图标（commit 1cb6263）
- 仓库 jincaiw/anySCP → jincaiw/anySSH（gh repo rename --yes）；旧 URL 301 重定向（git push 与 latest.json 下载均验证 200）；本地 remote set-url 更新。
- 签名私钥核对：secrets 完好（重命名不影响）、~/.tauri/anyssh-updater.key.pub 与 tauri.conf.json 内嵌公钥逐字节一致。
- 更新端点已改指向 anySSH（tauri.conf.json）；README 徽章等其余 URL 由用户自行同步。
- 新图标：ImageGen 生成（终端 >_ 光标 + 青紫渐变），平台强制水印用 PIL 采样填充清除（左移采样法）；`npx @tauri-apps/cli@2 icon` 全量再生成 25 个图标文件（含 android/ios 子目录）；app-icon.png / public/logo.png / public/favicon.png 同步。
- 发布 v0.14.6 为 Latest，latest.json 指向 0.14.6。

## README/URL 同步 + v0.14.7

- 应用户要求完成 URL 同步（原定用户自行处理）：README.md（badge/releases/clone/issues/discussions/star 共 9 处）、package.json repository、Cargo.toml repository/homepage、UpdateDialog.tsx 与 SettingsPage.tsx 的 REPO_URL，全部 jincaiw/anySCP → jincaiw/anySSH；clone 后 `cd anyssh` 也修正为 `cd anySSH`。
- 验证：`cargo fmt --check` 通过（仅改元数据与字符串字面量）；全仓 grep 确认无残留 jincaiw/anySCP。
- 提交 `a008727` 推送 jincaiw main，随后打 tag v0.14.7 发布。
- v0.14.7 构建首轮 E2E shard 4 挂：拉取 lscr.io/linuxserver/openssh-server 时 ghcr EOF（注册表抖动），rerun --failed 后过。
- CI 加固（commit `62730a2`）：Makefile 新增 `e2e-pull` 目标（compose pull --ignore-buildable，重试 5 次间隔 15s），ci.yml 在 "Run E2E suite" 前插入预拉取步骤。main CI（run 33648981714）全绿验证。此后注册表抖动只会延迟不会打挂 E2E shard。
