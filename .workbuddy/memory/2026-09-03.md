# 2026-09-03

## 背景（另一会话，09-02 深夜）

- v0.14.8 / v0.14.9 已发布（终端会话编码 + TERM 类型设置，及 WebKitGTK e2e 修复），详见 commit `2f33f7c` 日志。

## Windows WebView2 离线打包（v0.14.10）

- 需求：Windows 安装包内置 WebView2 运行时，不再安装时在线下载。
- 实现：tauri.conf.json 新增 `bundle.windows.webviewInstallMode = { "type": "offlineInstaller" }`（commit `a550b45`，rebase 到远端 v0.14.9 之上）。生效范围：NSIS（x64-setup.exe）与 MSI，安装器体积约 +127MB。
- **portable zip 无法内置 WebView2**：Tauri 便携包不含安装器逻辑，运行时仍需系统自带（Win11 内置；Win10 大多数已装）。需在 release notes 说明。
- 推送时发现远端领先（另一会话的 v0.14.8/9），rebase 后重推；教训：多会话并行工作时推送前先 `git fetch` 看分歧。
- v0.14.10 发布成功：setup.exe 9MB → 258MB（WebView2 离线运行时已嵌入，体积验证生效）。构建首轮 Linux job 在 tauri-action 上传末尾撞 GitHub API "Not Found - delete-a-release-asset"（8 资产已传完、仅剩 portable 步骤没跑），rerun --failed 即恢复；`gh release view --json assets` 对比 v0.14.9 体积确认打包成功。已发布 Latest，端点返回 0.14.10。
- 修复另一会话 feat（d19157f 终端编码切换器）导致的 main CI cargo fmt 失败：本地 `cargo fmt` 后提交 `885d9f5` 推送，CI（run 33705376403）全绿。该 feat 本身未发布 tag，归其会话收尾。
- 遗留问题巡检（应用户"检查并解决遗留问题"）：无草稿残留/无 open issue/PR、v0.14.10 资产 25/25、端点正常。唯一未决项：d19157f 在 main 上 8.5h 未发布（推它的会话已无动静）→ 补发 v0.14.11（run 33706291218，25 资产，已设 Latest，端点返回 0.14.11）。仍属用户侧的遗留：堡垒机实测、备份签名私钥。

## v0.14.11 发布（两会话协作收尾）

- 编码切换器 feat（d19157f）由本会话修 fmt（885d9f5）并补发 v0.14.11；另一会话同步后记录发布日志（8c0527d），两会话日志条目已合并。
- 坑：`gh release view --json isLatest` 字段不存在（只支持 isDraft/isPrerelease 等），Latest 状态用 `gh release list` 确认。

## 二次巡检 + 本地配置清理

- Dependabot 告警查询需 admin:repo_hook scope（gh auth refresh 交互授权），暂跳过。
- 本地 main 的 upstream 原指向 origin（上游 macnev2013），有误推风险且 status 显示 "ahead 46" 噪音 → `git branch --set-upstream-to=jincaiw/main main` 修正。
- workbuddy/main-ee9d69bc 是并行会话的 worktree 分支，不可删（git branch -d 报 worktree 占用）。

## v0.14.12 发布（2026-09-03 11:40）
- CI 33710392623 全绿（8m36s）→ tag v0.14.12 → Release 33710928654（19m11s，EXIT=0）
- 25/25 资产齐全；`gh release edit --draft=false --latest` 发布，标题 anySSH v0.14.12，双语 notes
- 端点验证：latest.json version=0.14.12（首次 curl 空响应为 CDN 传播瞬时问题，-L 后正常）
- 内容：交互式密码输入（PuTTY/Xshell 风格），commit 782d04f；等待用户实测堡垒机 420102-7@29.10.122
- 经验：updater 端点验证必须 `curl -sL`（未加 -L 可能拿到空 body 导致 JSON 解析失败）

## v0.14.13 发布：双因子堡垒机认证（2026-09-03 13:30）
- 用户提供堡垒机登录流程：KI 第一轮密码留空触发动态码挑战，第二轮输入"静态密码+动态码"拼接串
- 实现（commit cc3272f）：manager.rs 抽出 ki_attempt + KiAttemptResult 枚举，KI 失败后以 empty-first 策略重试（首个密码类提示答空，后续轮送拼接密码）；密码为空时跳过策略 B；prompt_asks_password 从 answer_auth_prompt 抽出
- 前端：PasswordPromptModal 增加双因子拼接提示（passwordDualFactorHint，中英）
- 验证：222 Rust + 133 前端测试全绿，clippy/tsc 干净；CI 33716955562（7m29s）→ Release 33717484360（19m41s）→ 25 资产 → Latest 发布 → latest.json=0.14.13
- 坑：node 脚本批量插入 i18n 行时误以分号结尾，tsc 报 TS1005 —— 插入行结尾要用逗号
- 等待用户实测堡垒机 420102-7@29.10.122：密码框输入"静态密码+动态码"拼接串

## 主机级终端编码 + LANG 环境变量 → v0.14.14 发布（2026-09-03 14:50）

- 需求：新建主机窗口增加终端编码设置；新增 env:lang 功能（全局默认 + 单连接覆盖、连接后生效、常用预设 zh_CN.UTF-8/en_US.UTF-8/C.UTF-8、堡垒机过滤 env 时降级）。
- 数据层：saved_hosts 迁移 15→16 加 `lang TEXT` + `terminal_encoding TEXT`（幂等 ALTER 模式）；upsert_host/list_hosts/get_host 同步加列（SELECT 索引 23/24）；TS SavedHost/HostConfig、Rust HostConfig（`#[serde(default)]`）加字段。
- 全局默认：settings-store 新增 `terminalLang`（persist key `terminal_lang`）+ LANG_PRESETS + LANG_RE（locale 白名单 `[A-Za-z0-9_.@-]`，≤32）；SettingsPage group.session 加"语言环境 (LANG)"可编辑下拉。
- 主机弹窗：起始目录下方新增"终端编码 + 语言环境"行（跟随全局 + 预设；LANG 用 CustomSelect editable 支持自定义）；HostsDashboard/HostEditModal 的 addSession 传 terminal_encoding 使 Session.encoding 初始值命中主机覆盖。
- SSH 应用路径（关键决策）：`manager.rs connect()` 用 valid_lang/encoding label 校验后把 per-host 覆盖合并进 SessionSettings；`session.rs open_pty/open_split_pty` 在 request_pty 后发 `channel.set_env(false, "LANG", …)`（vendored russh 支持）；堡垒机降级 = `encoding.rs with_lang_export()` 把 `export LANG=…; ` 拼在启动命令前经 shell 输入注入（env 请求被 AcceptEnv 过滤时仍生效）。
- 验证：cargo test 224 绿（新增 valid_lang/with_lang_export 测试）、vitest 133 绿、tsc/clippy/fmt 干净。
- 发布：commit 83b18a2 → main CI 33721602328（7m32s 绿）→ tag v0.14.14 → Release run 33723291022（19m29s，25 资产含 latest.json + Win portable）→ `gh release edit --draft=false --latest --notes-file`（notes 预写在 .workbuddy/temp/release-notes-v0.14.14.md）→ Latest 已生效，latest.json 返回 0.14.14。
- 待用户实测：堡垒机环境下 LANG 是否经降级注入生效（连上后跑 `locale` 验证）。

## 终端编码切换下拉智能翻转（2026-09-03 14:35）

- 用户反馈：终端右下角编码切换框（EncodingSwitcher，`absolute bottom-2 right-2`）下拉向下弹出，贴近窗口底边不好选。
- 根因：CustomSelect `computePos()` 恒定 `top: rect.bottom + 4`，从不判断视口剩余空间。
- 修复：CustomSelect 加 useLayoutEffect 翻转逻辑——列表挂载后测 `offsetHeight`，下方空间不足（`spaceBelow < listH + 8`）且上方空间更大时翻转为 `top: rect.top - listH - 4`（clamp 8px）；`flipDoneRef` 防循环，openDropdown 时重置。绘制前翻转无跳动；通用修复，所有底部锚定下拉受益。
- 验证：tsc 干净、vitest 133 绿（jsdom rect 全 0 时 spaceBelow=768 不触发翻转，测试不受影响）；e2e 因 Docker 未运行未执行，需后续 `make e2e` 补验（spec 66 编码切换）。
- 补验完成（15:00）：无需本地跑——该提交的 main CI 33724379377（含 E2E 四分片）已绿，与 `make e2e` 等效，遗留项关闭。

## v0.14.14 / v0.14.15 发布（2026-09-03 15:20）

- v0.14.14（另一会话的 per-host 编码 + LANG feat，83b18a2）构建完成 25/25 资产后由本会话发布 Latest，双语 notes，端点验证 0.14.14。
- 本会话提交 CustomSelect 翻转修复（75dc37e）→ main CI。**新坑：CI 显示 "cancelled" 且 `gh run rerun --failed` 生成新 run ID 与原 run 同并发组互相 cancel**——远端无新提交也可能发生并发抢跑，对原 run ID rerun 即可恢复，勿误判为测试失败。
- v0.14.15（下拉翻转修复）tag → Release run 33725920359（19m07s）→ 25/25 资产（latest.json + Windows portable 齐全）→ Latest 发布 → 端点返回 0.14.15。

## 终端配色主题功能 → v0.14.16 发布（2026-09-03 16:10–17:45）

- 需求：主题模型（背景/前景/光标/选中/ANSI 16色）、内置 6 套（Homebrew/One Dark Pro/Dracula/Solarized Dark/Tomorrow Night/Nord）、全局默认 + 单连接覆盖、自定义主题（取色器 + hex 输入）、导入 iTerm2 .itermcolors、实时生效 + 持久化。
- 新文件：`src/lib/terminal-themes.ts`（TerminalTheme 模型、SYSTEM_THEME_ID="system" 跟随应用主题、normalizeHex、parseItermColors DOMParser 解析（float + 8-bit int 两种分量格式）、sanitizeTheme）+ `src/components/settings/TerminalThemeSettings.tsx`（主题卡片网格 + 编辑器 Modal（每行 color input + hex 双输入）+ 文件导入 + ConfirmDangerDialog 删除）+ `src/lib/terminal-themes.test.ts`（14 测试）。
- settings-store：terminalThemeId（persist `terminal_theme_id`，默认 system）、terminalCustomThemes（persist `terminal_custom_themes` JSON，load 时 sanitizeTheme 过滤）；upsert/remove（删除默认主题时回退 system）。
- 实时应用：Terminal.tsx 订阅 globalThemeId/customThemes/themeMode + session.hostConfig.terminal_theme，effectiveTheme 变化时 `term.options.theme = getTerminalTheme(effectiveTheme)` 重绘；容器 backgroundColor 同步主题背景色；themeMode 依赖保证 system 哨兵在深浅色切换时也重绘。terminal-instances.getTerminalTheme(explicit?) 显式主题短路 CSS 变量路径，resolveSessionTheme(hostId 覆盖 → 全局)。
- 单连接覆盖：SavedHost/HostConfig 加 `terminal_theme`；Rust migration 16→17 加列 `terminal_theme TEXT`（幂等）+ upsert/list/get 查询索引 25；HostEditModal 终端区新增主题下拉（跟随全局/system/内置/自定义）；addSession 4 个调用点（HostsDashboard×2、HostEditModal、HistoryPage 用 getState读避免闭包过期）透传。
- i18n：settings.ts（terminal.group.theme + editor + 22 个颜色名 c.*）、host.ts（field.terminalTheme/Hint）中英双份；i18n-parity 测试绿。
- 验证：tsc 干净、vitest 147 绿（含新 14 测试）、cargo check + cargo test db:: 44 绿。
- 发布：提交前先跑 cargo fmt（自查出 2 处格式差异，避免 CI 挂）+ clippy 干净 → commit 07089cb → push 前 git fetch 确认无分歧 → main CI 33732061072 绿（9m22s）→ tag v0.14.16 → Release run 33733030286（16m14s，25/25 资产）→ `gh release edit --draft=false --latest --notes-file`（notes 在 .workbuddy/temp/release-notes-v0.14.16.md）→ Latest 生效，latest.json 返回 0.14.16（11 平台）。
- 坑：gh 无默认 remote（未 gh repo set-default），所有 gh 命令需 -R jincaiw/anySSH。

## v0.14.17：主题编辑器状态残留修复（2026-09-03 18:30）

- 复查发现 bug：ThemeEditorModal 常驻挂载（editing=null 时仅 ModalShell 返回 null，组件本身不卸载），useState 初始化器只跑一次——连续编辑两个不同主题时第二个显示第一个的旧名称/颜色。
- 修复：`<ThemeEditorModal key={editorState === "new" ? "new" : editorState?.id ?? "none"}>` 按编辑目标强制重挂载（commit 1e59e15）。附带走了一次 vite build 生产构建冒烟（6.78s，仅 chunk 体积 warning）。
- 验证：tsc 干净、vitest 22 绿（themes + parity）、CI 33735933980 绿（9m13s）→ tag v0.14.17 → Release run 33736823702（21m30s，25/25 资产）→ Latest 发布 → latest.json 返回 0.14.17（11 平台）。
- 经验：包一层"始终渲染的 Modal 包装组件 + 内部 useState 从 props 初始化"是 React 反模式，必须用 key 绑定目标 id 重挂载。

## 终端会话本地日志功能（全栈实现完成，未提交/未发布）

- 需求：记录终端全部输入输出，支持 ANSI 保留/剥离、全局/单会话/书签三种触发、按日期分目录 + 滚动切割 + 保留期/配额清理、密码打码、异步写入、0600 权限、内置查看器与导出。
- Rust 核心模块 `src-tauri/src/ssh/sessionlog.rs`：写线程（mpsc unbounded）+ MaskState 打码状态机（中英文提示词）+ strip_ansi 解析器（CSI/OSC/DCS/双字节 ESC）+ 清理 + list/read/export；`install_root` OnceLock 在 lib.rs setup 固定根目录。
- 挂接：`SshSession` 增加 `log: SessionLogContext`（open_pty/open_split_pty 传参），reader task 用克隆的 logger（避免 partial move）；`manager.connect` 收 `log_options`；`connect_saved_host`/`ssh_connect` 经 `resolve_auto_log_options(force, db)` 决定自动开录；书签强制标志经 `SavedHost.force_session_log`（DB migration 17→18）→ `HostConfig.force_session_log` 传递；split pane 继承源会话 logger。
- 7 个新命令已注册 lib.rs：ssh_start/stop_session_log、ssh_session_log_status、ssh_list_session_logs、ssh_read_log（tail 512KB/上限 4MB）、ssh_export_log、ssh_logs_dir。
- Rust 编译修复要点：spawn_writer 消费 options 需先 clone；commands start() 返回 PathBuf 需 .display().to_string()；测试代码三处 SavedHost/HostConfig 补 force_session_log。
- 单测修复（18 个 sessionlog 测试全绿，全量 242 绿）：strip_ansi 双字节分支先吞 peek 字符导致多删 1 字符（改为先扫 intermediates 再吞 final）；sanitize_component 的 `.` 仅在两侧均为字母数字时保留（阻断 `..` 与 `./`）；file_stem 日期改紧凑 `YYYYMMDD`；测试根 OnceLock 只能 install 一次 → 改为进程级共享根 + Mutex 串行化（原 TempRoot drop 会删掉共享根导致并行测试 fs 报错）；datetime_parts 时间加毫秒避免同 host/user 同秒文件名碰撞；cleanup 测试用 600KB×2 文件 + 1MB 配额（原 4KB 文件永远触不到 MB 级配额）。
- 前端：settings-store 7 个 session_log_* 设置；SettingsPage「会话日志」分组；HostEditModal 强制记录开关（force_session_log）；Terminal.tsx 右键菜单（pasteButton==="right" 时保持原右键粘贴，不显示菜单——保护 Terminal.clipboard.test.tsx 行为）；⌘⇧L 切换日志（AppShell + xterm 白名单）；SessionLogViewer 弹窗（列表/tail 预览/搜索高亮/导出 plugin-dialog/打开目录）；ui-store 增加 sessionLogViewerSessionId；i18n en-US/zh-CN terminal/settings/host 三文件 key 对齐（parity 测试过）。
- tsc 无错、vitest 147 全过、cargo test 242 全过。代码未 git commit。
- README 功能清单补充：SSH Terminal Client 小节新增 4 条 bullet（内置 6 套配色/22 槽位自定义/iTerm2 导入/单连接覆盖），commit 4c11443 推送 jincaiw/anySSH，CI run 33740628844 全绿（success）。注意：git remote 有两个——origin 指向 macnev2013/anySCP（无推送权限，403），推送必须用 `git push jincaiw main`。

## v0.14.18：会话日志功能发布（2026-09-03 18:50）

- clippy 13 警告清零：commands.rs 两处 ok_or_else→ok_or；sessionlog.rs unwrap_or_else(log_root)、sort_by→sort_by_key(Reverse)、9 处 assert_eq!(.., bool)→assert!/assert!。
- **首轮 main CI 失败**（run 33742528988）：sessionlog 测试 `logger_strip_ansi_option_produces_clean_text` flaky——多个测试共用 host="h"/user="u"，CI 高速并行下同毫秒 start 碰撞写同一文件（本地毫秒精度防护靠 SystemTime 不碰撞，但 CI 调度更快仍可能同毫秒）。修复：每个 logger 测试用唯一 host（hstop/hcast/hmask/hstrip/hkeep/hts），本地连跑 5 轮全绿（run 33743804329 通过）。
- 提交链：feat 12cfcad（29 文件 +2547）→ docs(memory) 0a07d99 → fix(test) d548867。tag v0.14.18 → Release run 33744578232（14m46s）→ 25/25 资产 → `--draft=false --latest --title "anySSH v0.14.18"`（标题沿用历史 "anySSH" 大小写）→ latest.json 返回 0.14.18（11 平台）。
- release notes 双语写临时文件经 --notes-file 上传；gh release edit 成功返回 tag URL。
- 经验：多测试共享命名空间（host/user/文件名）+ 时间戳命名 = 并行 flaky，测试夹具必须注入唯一标识而非依赖时间精度。

## 本会话追加（18:50 后）：CI actions Node24 升级 + sessionlog 生产侧加固

- 巡检通过：无草稿、无 open issue/PR、端点正常。
- CI actions 全部升到 Node24 运行时版本（checkout v5 / setup-node v5 / pnpm-action v5 / cache v5 / github-script v8 / buildx v4 / build-push v7），commit 5c67497。原因：Node20 运行时 2026-09-16 起从 runner 移除。本轮 33745325665 全绿验证。
- sessionlog flake 双会话同诊：另一会话已修测试侧（d548867，唯一 host，v0.14.18）。本会话补生产侧加固 7925a9f：LOG_SEQ 进程内序号并 bake 进文件名 stem（start() 取一次、rotation 复用），同 host+user 同毫秒 start（重连风暴）在 app 内也不再撞文件；测试 hstrip/hkeep/hrot 保持独立命名。CI 33745325665 全绿。
- 教训补充：文件名毫秒精度 + append 打开 = 跨测试/跨会话污染通道，唯一性要靠显式序号而非时间精度；stop() 的 3s recv_timeout 超时窗口与并发 guard 是排查时的红鲱鱼。

## 全量体检 + README/协议/清理（19:20–19:35）

- 功能验证全绿：cargo fmt/test(243)/clippy 0 警告、tsc/vitest(147)/vite build 冒烟；main CI（最新提交 306b434，含 E2E 四分片）绿。条 "failed" run 实为并发抢跑 cancelled（33746187585），无害。
- 修复测试卫生 bug：Terminal.clipboard.test.tsx 触发右键菜单时走真实 session-log 动态 import（@tauri-apps/api/core），微任务在环境 teardown 后执行报错 → vi.mock("../../lib/session-log") 隔离；147 测试全绿无 teardown error。
- sessionlog.rs 安全复审通过：resolve_log_path 有 relative+canonicalize+前缀校验（防路径遍历），mask/rotation/cleanup 均有测试覆盖。
- **LICENSE 之前不存在**（README/package.json 声明 MIT 却无文件）→ 新建 MIT LICENSE，双版权行（2025 Nevil Macwan anySCP + 2026 jincaiw anySSH）。上游 macnev2013/anySCP **无 LICENSE 文件**（GitHub license:null），仅 README 文字声明 MIT —— 法律风险已通过保留上游版权声明 + README/致谢声明 fork 来源缓解，剩余理论风险：上游从未实际提供许可证文本。
- vendor/russh（Apache-2.0）原缺 LICENSE 文本 → 从 warp-tech/russh LICENSE.txt 补入 vendor/russh/LICENSE。
- 清理：git rm src-tauri/vendor/FreeRDP（孤儿 gitlink，无 .gitmodules 无代码引用）、public/fonts/geist.zip（9 字节占位残留）；screens/anyscp.gif → anyssh.gif（对齐 Makefile/build-assets.sh 工具链输出名，同时修复 README 原引用断链）；.gitignore 增加 .workbuddy/backups/、.workbuddy/temp/；FUNDING.yml github: macnev2013 → jincaiw；README visitor badge page_id anySCP→anySSH。
- README：英文版顶部加语言切换行（English | 简体中文）+ 补会话日志功能条目 + Acknowledgments 声明 fork 自 anySCP；新建完整中文版 README.zh-CN.md。
- 未提交推送（等待用户确认后由发布流程收尾）。
