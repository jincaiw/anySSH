# 2026-09-04

## 堡垒机空密码触发短信修复 → v0.14.20（09:30–10:10）

- 用户实测反馈：空密码连接**不会**触发服务端下发短信验证码；输入任意错误密码**可以**触发（v0.14.13 的"首个密码提示答空"策略与该堡垒机实际行为不符，且密码为空时策略 B 被整体跳过）。
- 根因：`auth_with_password` 中 `if empty_first && password.is_empty() { break; }` 跳过策略 B + 策略 B 触发应答恒为空串。
- 修复（commit 7442de0）：新增 `EMPTY_PASSWORD_TRIGGER = "anyssh"` 常量与 `dual_factor_trigger_answer(password)` 纯函数——策略 B 触发应答改为"与策略 A 不同的值"：有密码→空串（保留原语义），空密码→非空占位值（满足"非空应答才触发短信"类堡垒机）；策略 B 不再因空密码跳过。策略 B 仅在 A 被拒后执行，普通主机不受影响（B 被拒后回落 plain password）。
- 测试：新增 `empty_password_trigger_is_a_non_empty_dummy`（触发值非空断言 + 空密码端到端应答走查），改写 `empty_first_strategy_triggers_then_answers_password` 复用新函数。244 Rust 测试绿、clippy/fmt 干净、tsc/i18n parity 绿。
- 前端：passwordDualFactorHint（中英）补充"若尚未收到短信验证码，可先留空密码连接一次以触发短信下发"。
- 发布：main CI 33826559530 绿（12m）→ tag v0.14.20 → Release run 33827263380（15m，25/25 资产）→ Latest 发布，双语 notes（.workbuddy/temp/release-notes-v0.14.20.md）→ 端点返回 0.14.20。
- 待用户实测：① 空密码连接一次 → 手机收短信；② 重连输入"静态密码+动态码"拼接串。
- 发布后巡检：v0.14.20 为 Latest、端点 0.14.20、无 open issue/PR；7442de0 与 4aec481 的 CI 均绿。全部闭环，等待用户堡垒机实测结果。

## 空密码仍不触发短信：两道前端/后端闸门 → v0.14.21（11:18–11:55）

- 用户实测 v0.14.20 反馈：身份认证弹窗和编辑主机页空密码连接仍不触发短信，错密码可以（附截图，弹窗已显示 v0.14.20 新提示文案，确认用户已升级）。
- 根因（两道闸门，均早于 v0.14.20 的后端修复生效点）：
  1. **PasswordPromptModal.tsx 前端校验**：`if (!password)` 报"密码不能为空"，空提交直接被拦，连接请求根本不发出。
  2. **commands.rs resolve_auth_method**：空 password_override 被 `filter(|p| !p.is_empty())` 丢弃回落 vault；vault 中空保存密码直接报错（此前"严格化"修复所为）。
- 修复（commit d22cd82）：弹窗允许空提交（移除 error 状态及未用的 passwordRequired i18n 键）；resolve_auth_method 空 override 按字面采用、vault 空密码放行（vault **读取失败**仍报错，不回退空串）。has_saved_password 保持空=false → 弹窗。
- 完整链路（修复后）：空密码连接 → 策略 A 全空应答（不触发）→ 策略 B 触发提示答 "anyssh"（非空）→ 服务端下发短信 → 用户重连输入"静态密码+动态码"。
- 验证：244 Rust + 147 前端测试绿、clippy/fmt/tsc 干净、无 e2e 依赖旧校验。发布 v0.14.21（25/25 资产、Latest、端点 0.14.21）。
- 教训：v0.14.20 只修了 ki_attempt 内部逻辑，未端到端走查"空密码从 UI 到 SSH 层"的完整链路——前端校验和 resolve_auth_method 的历史拦截都让修复不可达。涉及认证行为的修复必须沿数据流全链路验证。
- 发布后巡检：v0.14.21 为 Latest、无 open issue/PR；d22cd82 与 fa688e4 的 CI 均绿。全部闭环，等待用户升级 v0.14.21 后实测空密码触发短信。

## 真正根因：弹窗密码从未到达后端 → v0.14.22（12:32–13:10）

- 用户实测 v0.14.21 仍不触发：身份认证弹窗空密码/错密码都不行；编辑主机空密码报 "no saved password"（截图），错密码（保存进 vault）可以。用户用的是 **portable** 构建（截图标题 x86_64-portable）。
- 真正根因（v0.14.12 起就存在，三条路径全坏，且零 e2e 覆盖）：
  1. **connectToHost/handleRecentConnect/exploreHost 拿到弹窗 secrets 后仍重跑 ensurePasswordOrPrompt** → vault 无密码 → 弹窗无限重弹，输入的密码从未使用。这解释了"弹窗输什么都触发不了"。
  2. **HostEditModal 连接按钮**直接 invoke connect_saved_host，不接密码弹窗 → 无保存密码时本地报错（截图 2）。
- 修复（commit c14e68f）：三处连接函数 `!secrets &&` 短路 vault 检查；HostEditModal handleConnect 接入 has_saved_password 检查 + 内嵌 PasswordPromptModal（secrets 作 override 透传，remember 由后端保存）；按钮 onClick 包一层避免 event 误作 secrets。
- 修复链路全景（三层）：UI 层弹窗密码传递（本修复）→ 认证解析层空密码放行（v0.14.21）→ KI 策略 B 非空触发应答（v0.14.20）。
- 验证：tsc/147 vitest/vite build 绿、CI 33837627757 绿 → v0.14.22（19m37s，25/25 资产，Latest，端点 0.14.22）。
- 教训：交互式密码弹窗是 v0.14.12 引入的功能，从未有 e2e/unit 覆盖，"弹窗提交→连接"的循环 bug 存在四个版本未被发现。密码弹窗路径需要 e2e spec（备忘）。
- 注意：用户用 portable 构建，auto-updater 可能不适用，提醒从 Release 页下载新 portable 包并核对版本号。
- 收尾巡检：v0.14.22 为 Latest、端点 0.14.22、无 open issue/PR；c14e68f 与 7351e13 的 CI 均绿。全部闭环，等待用户用 v0.14.22 portable 包实测。
- 备忘（后续改进）：密码弹窗"提交→连接"路径无任何测试覆盖（三轮发布才定位），值得补一条 e2e spec（mock SSH 服务 + 空密码提交断言连接发起）。
- ⚠️ 13:20 起工作区出现另一并行会话的未提交变更（sessionlog.rs/session.rs/commands.rs/session-log.ts，约 +205/-36，会话日志功能增强中）——本会话不触碰、不提交、不还原，由该会话自行收尾。

## 登录流程优化：触发后弹窗自我说明 → v0.14.24（13:31–14:10）

- 用户确认：**空密码连接已能触发服务端下发短信**（v0.14.22 修复生效，截图轨迹显示 "ki retry: first password prompt answered with the dual-factor trigger"）。要求优化登录流程。
- 优化（commit 6d62b66）：① AuthOutcome 增加 dual_factor_attempted 标志，空密码失败时错误后缀由误导性的 "saved credential is missing" 换成 "dual-factor trigger sent: reconnect and type <static password><dynamic code>"；② ConnectionDialog / HostEditModal 检测该 marker，在技术轨迹上方优先显示本地化引导横幅（强调色信息样式，非红色报错），i18n key connect.dualFactorTriggered（中英）；③ backend-errors.ts 导出 DUAL_FACTOR_TRIGGER_MARKER + dualFactorHintOf 助手。
- 验证：246 Rust（含并行会话新增 2 个）+ 147 前端测试绿、clippy/fmt/tsc 干净。注意 cargo fmt 触碰了并行会话未提交的文件（全 crate 格式化），无冲突。
- **双会话发布协调**：并行会话已发 v0.14.23（Xshell 式日志增强，草稿 16/25 资产→构建完成 25/25）。本会话发布其草稿为普通 release（不带 --latest，但 GitHub 自动把最新发布标为 Latest，属临时状态），随后发布 v0.14.24 为 Latest 接管。教训：release.yml 无并发组，两个 tag 构建可并行；发布他人草稿时 --latest 语义要看清，最终以 endpoint version 验证为准（0.14.24 ✓）。
- 用户侧提醒：portable 版需手动下载 v0.14.24；触发流程为 空密码连接（收短信）→ 重连输 静态密码+动态码。
# 2026-09-04

## Xshell 风格会话日志增强（启动另存为 + 打开日志文件，未提交）

- 背景：v0.14.18 已有对已开会话启/停日志（右键菜单 + ⌘⇧L + 全局/书签强制），用户对照 Xshell 截图要求补齐：启动时「另存为」选路径、打开日志文件。
- Rust：`SessionLogger::start` 加第 4 参 `custom: Option<PathBuf>`，贯通 spawn_writer→writer_loop→open_log_file→write_chunk（rotation 时对自定义路径用 `with_part_suffix` 在扩展名前插 `_partN`，自动创建父目录，仍 0600）；`ssh_start_session_log` 命令加可选 `path` 参数（commands.rs 补 `use std::path::PathBuf`）。
- 前端：session-log.ts `toggleSessionLog(sessionId, path?)` + 新增 `startSessionLogWithSaveDialog`（plugin-dialog save，默认路径 `logs_dir/host_YYYY-MM-DD_HH-MM-SS.log`，取消则不动）+ `revealActiveLogFile`（revealItemInDir 定位当前日志文件）；非 React 语境直接 `import { t } from "../i18n"`（项目自研 i18n 导出非响应式 t，勿用 require hack）。Terminal.tsx 菜单 Xshell 化：启动…（Disc 图标）/停止（Square）、active 时插入「打开日志文件」（FileText）。
- i18n：zh-CN/en-US terminal.ts 更新 menu.startLog（加省略号）+ 新增 menu.startTitle、menu.openLogFile。
- 验证：Rust 246 tests（+2：custom path 落盘 0600、with_part_suffix）+ clippy -D warnings 清零（修 1 处 OsString useless_conversion）；tsc 干净、vitest 147 过。
- 教训：perl 批量改签名后仍有 4 处调用点漏改（session.rs ×2、commands.rs import、rotation 测试），编译器全数兜住——改函数签名后先 grep 全部调用点再收尾。

## v0.14.23 发布：Xshell 风格会话日志增强

- 提交链：feat 2525057（7 文件 +242/-46）→ docs(memory) 98f02eb。tag v0.14.23（注意并行会话已发到 v0.14.22，发版前必须 fetch 确认下一版本号）→ CI run 33840278858 绿（9m27s）→ Release run 33840907298（19m40s）→ 25/25 资产 → Latest 发布 → 端点返回 0.14.23。

## 修复 spec 66 编码持久化 e2e 竞态（main CI 红灯根因）

- main CI 连续两次失败（bba663f/2470521）均折在 E2E shard 4/4 spec 66："expected 'utf-8' to equal 'gbk'" at line 84——GBK 选择生效但 relaunchApp 后持久化丢失。
- 根因：settings-store 的 `persist()` 是 fire-and-forget（动态 import invoke("save_setting")），测试在 DOM 更新后立即 `browser.reloadSession()`，异步 IPC 落盘被重启打断。TERM 同结构测试靠运气通过；98f02eb 同代码曾绿（高负载冷启动扩大竞态窗口）。
- 修复（commit dd92f53，helper 级，覆盖 7 个 relaunchApp spec）：
  1. settings-store.ts：`inFlightPersists` Set 追踪每个 persist promise + 导出 `flushPendingPersists()`（循环等 allSettled 直到清空）+ 挂 `window.__anysshFlushSettings` 供 WebDriver 页面上下文调用。
  2. reset.ts `relaunchApp()`：reloadSession 前先 `browser.execute` 调用 flush，确保所有 save_setting 落盘。
- 验证：tsc 干净、settings-store 10 单测绿；CI run 33844426170 全绿（9m13s，E2E 4/4 shard 全 success，spec 66 通过），main CI 恢复绿灯。
- 纯测试修复不发版（Latest 仍为 v0.14.24，资产完整不受影响）。

## 补齐密码弹窗路径 e2e 覆盖（spec 67）

- 背景：堡垒机三轮修复（v0.14.20–22）暴露密码弹窗路径零 e2e 覆盖（弹窗密码被吞、无限重弹、HostEditModal 不接弹窗三个 bug 全靠用户实测发现）。
- 新增 tests/e2e/specs/67-password-prompt-modal.spec.ts，对真实 sshd-pass 目标锁定三条路径：
  1. 无保存密码点连接 → 弹出 PasswordPromptModal（不是报错）+ 双因子提示可见；
  2. 空密码提交不被拦 → 后端失败消息含 "dual-factor trigger sent"（策略 B 已运行）；弹窗不重开、host modal 保持打开；
  3. 输错密码 → 一次拒绝且无重弹循环；输对密码 → 终端挂载并跑 echo 哨兵命令（证明弹窗密码到达 russh）。
- 关键前提验证：linuxserver/openssh-server 镜像复制 Alpine 原版 sshd_config 只改 PasswordAuthentication/Port；OpenSSH 上游默认 KbdInteractiveAuthentication=yes，且无 PAM 时 auth2-chall.c 有 passwd_device 回退——KI 会被提供，策略 B 必运行，marker 必出现。CI 实测通过证实。
- CI 揪出一个 WebKitWebDriver 坑：`setValue("")` 空字符串触发 "Missing text parameter"（element/value 协议不接受空 text）——空提交场景跳过 setValue（输入框初始即空）。修复 commit 6500268。
- 结果：run 33846544100 全绿（8m14s，spec 67 在 shard 1/4 通过）。main CI 绿灯 + 密码弹窗路径覆盖闭环。

## 空密码报错三分支区分 → v0.14.25

- 缺口：spec 67 验证时发现——服务器不提供 keyboard-interactive 时策略 B 从未运行，但空密码连接的报错仍说 "the saved credential is missing"，对故意空密码触发短信的堡垒机用户是错误指引。
- 修复（commit 52245e5）：新增 `KI_NOT_OFFERED_TRAIL` 常量 + 把空密码后缀抽成 `empty_password_suffix(&AuthOutcome)` 纯函数，三分支：① 策略 B 已运行 → "dual-factor trigger sent: ..."（引导）；② KI 未提供 → "the dual-factor trigger was NOT sent: this server does not offer keyboard-interactive"（不归咎凭据）；③ 其余 → "the saved credential is missing"。新增单测 `empty_password_error_suffix_reflects_what_happened`（含"不与前端 trigger sent marker 撞车"断言）。
- 教训：消息措辞（"NO ... was sent"）与测试断言（"was NOT sent"）不一致导致测试失败一轮——先定断言字符串再写消息。
- 发布：main CI 33847760666 绿（8m12s）→ tag v0.14.25 → Release run 33848480260（18m37s）→ 25/25 资产（含 latest.json + Windows portable）→ Latest 发布（notes: .workbuddy/temp/release-notes-v0.14.25.md）→ 端点返回 0.14.25。发布后巡检：v0.14.25 Latest、0 open issue/PR。

## 收尾巡检与知识沉淀

- 确认 ci.yml 的 Frontend job 跑 `pnpm test`（vitest）——settings-store flush 改动已被 CI 全量前端测试覆盖，无需本地重跑。
- WebKitWebDriver `setValue("")` 空文本坑（"Missing text parameter"）已沉淀进 anyssh-release-flow skill 的 WebKitGTK 段落。
- e2e README 补充 relaunchApp 的 persist flush 行为说明（commit 7a39de6），main CI 33850281158 绿（13m）。
- 本日最终状态：v0.14.25 Latest，main CI 全绿，0 open issue/PR，无遗留待办。

## 堡垒机 TUI 退格键无效 → v0.14.26（16:02–17:00）

- 用户实测：老式 curses 维护终端（29.1.6.122 变更单录入界面）SD 字段输入 12345 后 Backspace 删除无效。
- 根因：xterm.js Backspace 默认发 `\x7f`（DEL），全链路（onData→ssh_send_input→PTY 管道）无按键翻译；老式 curses TUI 只认 `\x08`（^H）。PuTTY/Xshell 同款场景的经典开关。
- 方案评估：A 可配置开关（✅业界标准）；B 全局默认改 ^H（❌破坏现代应用）；C 换 TERM=vt100（❌字节不变无效）；D 连发两字节（❌双删）。用户选定 A 的"全局+每主机覆盖"粒度。
- 实现（commit 5660c15，16 文件 +251）：
  - DB 迁移 18→19：saved_hosts.backspace_sends_ctrl_h（可空 INTEGER，NULL=跟随全局）
  - settings-store：terminalBackspaceCtrlH 全局设置（terminal_backspace_ctrl_h）+ 设置页 SegmentedControl（s-backspace）
  - terminal-instances：createEntry 解析"每主机覆盖 ?? 全局"，attachCustomKeyEventHandler 拦截普通 Backspace 改发 `\x08`（组合键不动）；抽出 sendBytes 复用
  - HostEditModal 三态选择（host-modal-backspace）；HostsDashboard ×2 + HistoryPage addSession 透传（注意用 `?? undefined` 保 false，勿用 `||`）
  - i18n 中英（settings.terminal.backspace* + host.field.backspace*）
- 测试：Rust 248 绿（新增 backspace_override_round_trips + NULL roundtrip 断言）；vitest 16 绿（新增 3 个）；tsc 干净；e2e spec 66 增加开关持久化用例（SegmentedControl testid 是 s-backspace-ctrlh/del，容器本身无 testid）。
- 踩坑：settings-store.test.ts 插入新 describe 时把原 describe 从中间截断（结构破坏），插入位置应在文件末尾或完整 describe 边界之后。
- 发布：main CI 33853366386 绿（11m，E2E 4/4 含 spec 66 新用例）→ tag v0.14.26 → Release run 33854402814（19m）→ 25/25 资产 → Latest 发布 → 端点 0.14.26（11 平台全签名）。巡检：Latest、0 open issue/PR。
- 待用户实测：编辑该堡垒机主机 → 退格键行为选 Ctrl+H → 重连后 Backspace 应可删除。

## 一键双因子登录（17:00–18:00）→ v0.14.27 已发布

- 用户需求：主机页点击连接 → 服务端自动发短信 → 认证窗输入"静态密码+动态码"拼接串 → 直接登录。评估后用户确认：触发时机选 **B 自动记忆**（连接错误含 dual-factor 标记时记住主机），并加**重新发送短信按钮**。
- 设计要点：
  - 后端新增 `trigger_dual_factor_sms` 命令（ssh/commands.rs）：空密码构造 config（`Some("")`）→ 60s timeout 内 state.connect → `Ok(Err)=Ok(false)`（预期失败=短信已触发）、意外成功则立即 disconnect、超时 warn。
  - 标记信号：`DUAL_FACTOR_TRIGGER_MARKER = "dual-factor trigger sent"`（仅空密码连接失败出现）——窄标记避免普通主机误标。backend-errors.ts 加 `isDualFactorTriggerError`。
  - settings-store：`dualFactorHostIds`（persist dual_factor_hosts JSON）+ `markHostDualFactor`（去重）。
  - HostsDashboard：marked 主机在 vault 检查**之前**短路 → fire trigger（fire-and-forget）+ armed 弹窗并行（保存了静态密码也弹窗——静态密码无 OTP 直连必败）；connectToHost/handleRecentConnect catch 里 `noteDualFactorError`。
  - HostEditModal：`handleConnect` 分支 `(dualFactorMarked || !form.password)` 时 fire+armed 弹窗；catch 标记。
  - PasswordPromptModal：armed 时 hint 换 armed 文案、隐藏 remember checkbox（拼接串不可保存）、footerStart 加 Resend 按钮（RefreshCw + 3s 防抖）。i18n `connect.passwordPromptArmedHint`/`passwordResend`。
- 踩坑：
  - `state.disconnect(&session_id)` E0308——SessionId newtype 要 `.0`，AppHandle 按值。
  - tsc 抓到 HostEditModal catch 块引用 try 内 `const host`（作用域外）——把 `buildHost()` 提升到 try 之前修复。
  - vitest 时序：同步连发 mark 后 `toHaveBeenLastCalledWith` 收不到中间 persist（fire-and-forget 微任务与 waitFor 竞争）——改串行 waitFor + mockClear 验证去重。
- 验证：Rust 248 绿、clippy 干净、tsc 干净、vitest 153/153 绿（全套 23 文件）。commit 17e1cab 已推 main。
- 发布：main CI 33858360072 绿（8m）→ tag v0.14.27 → Release run 33859090209（17m）→ 25/25 资产（含 latest.json + windows portable zip）→ Latest 发布 → 端点 0.14.27（11 平台全签名）。

## 配色方案独立分区 + 修复（18:14–）→ v0.14.28

- 用户需求：设置页的"配色方案"拆成独立分区（导航放在"终端"下面），并对该功能详细检查评估、修复问题。
- 检查评估发现并修复 4 个问题：
  1. 新建主题种子 bug：注释称从"当前选中的内置/自定义主题"播种，但只查 terminalCustomThemes——选中内置主题（如 Dracula）时错误落到 One Dark Pro。修复：改用 resolveTerminalTheme。
  2. resolveTerminalTheme 回退用魔法索引 BUILTIN_THEMES[1]——重排数组会静默改变悬空引用的默认主题。修复：按 id "one-dark-pro" 查找 + [0] 兜底。
  3. .itermcolors 解析：Number("") === 0，空的 Red/Green/Blue Component 值被静默当 0（纯黑）。修复：跳过空字符串（整个颜色 dict 跳过 → 走默认回退）。
  4. 预览卡不演示选中色——ThemePreview 的 user@host 部分改用 selectionBackground/Foreground 渲染，损坏的选中色对肉眼可见。
- 结构改动：SettingsPage SectionId 加 "themes"（icon SwatchBook，lucide 已确认存在），SECTIONS/SECTION_DESCRIPTIONS/SectionContent 各加一项；TerminalSettings 移除 theme SettingsGroup；i18n 加 sections.themes(.description)、删 terminal.group.theme（成对，parity 测试过）。
- 测试：terminal-themes.test 15 绿（新增空分量用例 + 回退按 id 断言）；i18n parity 8 绿；全套 vitest 154/154；tsc 干净。e2e spec 61 新增"themes 分区切换 Dracula + 重启持久化"用例。
- 发布：首轮 CI 33863768544 失败——新 e2e 用例选择器 bug（theme-card-* 的 aria-pressed 在内层 button 上，data-testid 在外层 div，getAttribute 永远 null）。修复后 d33b75e，CI 33864557892 绿（8m）→ tag v0.14.28 → Release run 33865213803（19m）→ 25/25 资产 → Latest 发布 → 端点 0.14.28（11 平台）。
- 踩坑：同一文件的两个 Edit 并行调用发生覆盖丢失（第一批的第一个 edit 被第二个基于旧内容的写回冲掉）——同文件多处编辑必须串行。

## 全功能审计（20:50–22:25）→ v0.14.29 已发布

- 用户要求：详细全面检查评估所有功能是否正常并修复 bug，重点近期新增功能。
- 方法：4 个 Explore 子代理并行审计（双因子、退格键、配色方案、核心回归）+ 我逐条核验后修复。教训：子代理结论必须自己读代码复核（本次全部属实，但有一处需重新定位）。
- 修复的确认缺陷（15 项）：
  核心：① Explore 泄漏 SSH 连接（sftp_close/scp_close 不碰 SshManager；新增 closeSftpSession 统一三处关闭路径）② handleRecentConnect 依赖只写 [t]（t 按 locale memo 永不变，回调冻结在首次渲染 → 按主机覆盖全丢 + 密钥主机误弹密码框）③ useSshOutput 监听器注册竞态泄漏（listen resolve 期间卸载 → unlisten undefined → 回调会为已销毁会话重建 xterm）④ 强调色自定义防抖与预设覆盖（setAccentHue 需 clearTimeout）⑤ invalid_encoding 缺 i18n 词条。
  退格键：① IME 组合态被劫持（xterm 先跑自定义 handler 再跑 CompositionHelper，拼音删字会发 ^H；加 isComposing + keyCode 229 守卫）② Shift+Backspace 误改写 ③ 设置对已开会话不生效（改为每次按键实时解析，删除 TerminalEntry 死字段）。
  双因子：① 触发无去重（按 hostId 的 in-flight Set）② 结果不可区分（后端改返回 sent/connected/unreachable/timeout，UI 不再谎报已触发）③ 无法取消标记（新增 unmarkHostDualFactor + 删除主机清理 + 弹窗"不再自动触发"）④ 弹窗未 key 化（A 的密码会带到 B）⑤ 重发定时器未清理 ⑥ HostEditModal 重发用了 NEW_HOST 哨兵 id ⑦ 持久化列表未清洗。
  配色方案：① 删除主题后主机悬空覆盖回退 One Dark Pro（改为回退全局，resolveSessionTheme 与 Terminal 对齐）② terminal_theme_id 未校验 ③ 自定义主题 id 撞内置/system ④ .itermcolors 混合 scale 变黑（改按通道）⑤ 导入无限体积 + 同名堆积 ⑥ 强调色变更/调色板切换后终端不重绘（加 accent 依赖 + term.refresh）。
- 踩坑：新增 i18n key 时调用处前缀不一致（PasswordPromptModal 用 dashboard.connect.*，HostsDashboard/HostEditModal 漏了 dashboard. 前缀）→ i18n-parity 测试 "resolves every statically referenced key" 会抓到。另：store 测试里持久化命令名是 save_setting 不是 set_setting。
- 验证：Rust 248 绿、clippy 干净、tsc 干净、vitest 161/161（新增 7 例）。commit f2a25df → main CI 33880530423 绿（10m，E2E 4/4）→ tag v0.14.29 → Release 33881559229（20m）→ 25/25 资产 → Latest → 端点 0.14.29（11 平台）。

## 第二轮全功能审计（23:00–）→ v0.14.30（进行中）

- 用户再次要求详细全面检查评估所有功能并修复 bug。方法同前：并行 Explore 子代理 + 我逐条读码复核。本轮覆盖上一轮（v0.14.29）未检区域：分片重连、S3 桶导航、端口转发 SSH 生命周期、SCP/SFTP 传输清理。
- 已确认并修复：
  ① DisconnectOverlay 分片重连 bug（v0.14.30）：原 handleReconnect 把 sessionId 当 tabId 传给 useTabStore.removeTab，分片布局下误删活着的 sibling tab + 泄漏其连接。改为先 removeSession(sessionId)，仅当 session-store tab 已消失才 removeTab(tabId)（对齐 handleClose 已验证逻辑）。
  ② S3 双击加载：selectBucket 显式 loadObjects("") 与 currentBucket effect 重复触发 s3_list_objects。去掉 selectBucket 里的 loadObjects，由 effect 精确触发一次。
  ③ S3 无法返回桶列表：对象视图无出口。s3-store.setCurrentBucket 类型放宽为 string|null；ExplorerToolbar 加可选 onShowSessionRoot/sessionRootLabel（SFTP 不传不受影响），S3Browser 对象视图左侧出现 "← 存储桶" 按钮 → setCurrentBucket(id, null)。
  ④ 端口转发 SSH 会话泄漏：pf_start_tunnel 用 connect_no_pty 建 SSH 会话但 stop/restart/delete/bind-fail 从不断开。ActiveTunnel 记录 ssh_session_id；stop_tunnel 返回被停会话 id；pf_stop_tunnel / pf_delete_rule / pf_start_tunnel（restart 替换 + 失败清理）均调 ssh_manager.disconnect。
  ⑤ SCP 目录上传对单文件 stat 失败静默 continue（丢文件）：改向上传播 LocalIoError 使整 job 失败并在面板可见。
  ⑥ SFTP 单文件下载错误/取消只清部分文件于取消路径：重构成 download_file_to_disk + 外层包裹，任何 Err 都 remove_file 清部分文件。
- 验证：Rust 248 绿、clippy 干净、cargo fmt 干净、tsc 干净、vitest 165/165（新增 s3-store currentBucket 导航 4 例）。
- commit 38f437e → main CI 33890008328 绿（8m，E2E 4/4）→ tag v0.14.30 → Release run 33890904085 绿（20m）→ 25/25 资产 → Latest 发布 → 端点 0.14.30。发布后巡检：Latest、0 open issue/PR；docs(memory) 4d262cd 已推 main。

## 堡垒机登录流程检查 + 认证弹窗精简（09-05 10:16–）→ v0.14.31

- 用户期望流程：①点主机卡片触发短信下发 ②弹窗输入静态密码+动态码拼接 ③直接登录。走查确认三条路径（HostCard 点击/最近连接/HostEditModal 连接）对已标记主机均已是此行为（ensurePasswordOrPrompt 短路 → armed 弹窗 + fireDualFactorTrigger 并行），链路无需修。
- 本次改动（commit cbb48db）：PasswordPromptModal 删掉两段小字提示 + 弹窗内"不再自动触发"虚线链接；armed 模式标签改"静态密码 + 动态码"、占位"请输入静态密码+动态码，直接拼接"（标签即说明）；"取消自动发送短信验证码"挪到 HostCard 右键菜单（reactive 读取 dualFactorHostIds，仅标记主机显示）。
- i18n：新增 connect.passwordArmedLabel/passwordArmedPlaceholder + hostCard.disableDualFactor，删 passwordDualFactorHint/passwordPromptArmedHint/passwordDisableAuto（中英成对）。e2e spec 67 改用 remember checkbox 断言普通弹窗。
- 验证：tsc 干净、vitest 165/165。纯前端无 Rust 改动。
- 发布：main CI 33938913023 绿（8m）→ tag v0.14.31 → Release 33939332587 绿（18m）→ 25/25 资产 → Latest → 端点 0.14.31。巡检：Latest、0 open issue/PR。
