# 2026-09-05

## anySSH 多协议扩展方案（Telnet/Serial/Terminal/RDP/VNC）
- 调研 navop（feigeCode/navop）：许可证禁止商业转售 → 只参考不移植；确认其上游依赖：serialport v4、自研 Telnet（IAC 协商 + TelnetLoginScript expect/send）、RDP 用 IronRDP（helper 进程方式）、russh 0.63、alacritty_terminal 0.26（anySSH 不需要，xterm.js 已承担仿真）。
- 完成《docs/multi-protocol-expansion-plan.md》：P0 term 抽象层重构（TerminalBackend trait + term:* 事件 + DB 加 kind/params_json 列）→ P1 本地终端（portable-pty）+ Telnet（自研）→ P2 Serial（serialport）→ P3 VNC（vendor rust-vnc fork 现代化）+ FrameComposer 帧管线（rd_fetch_frame 二进制拉取 + rd:frame-ready 事件唤醒）→ P4 RDP（ironrdp-connector/async/session/graphics/input/tls 进程内集成，NLA 注意 MSRV 1.89 与跨平台 NTLM 风险）。总估 36–52 人日。
- 关键架构决策：RDP/VNC 帧走 tauri::ipc::Response 二进制（避免 event base64），拉取制背压；Terminal.tsx 换数据源零 UI 改动；ssh_* 命令双轨保留一个版本周期。

## v1.1 再评估（全网复核）
- **VNC 切换方案**：rust-vnc fork → **noVNC 前端内嵌（vendor core/）+ 后端 tokio-tungstenite WS↔TCP 桥**（127.0.0.1 + 一次性 token）。noVNC（MPL-2.0，Cendio 维护）自带 Tight/ZRLE/H.264/VeNCrypt 全套。P3 降至 5–7 人日。回退条件：R11（WKWebView ws:// 混合内容拦截，P3 首日 spike 验证）。
- **RDP 维持原生 IronRDP**：复核 iron-remote-gui/ironrdp-web（WASM Web 客户端，Devolutions 生产在用）——但其 connect() 绑定专有 Devolutions Gateway（RDCleanPath），脱离网关不可用，否决"webview 统一路线"。
- 帧通道补实测数据：tauri::ipc::Response ~225Mbps（Windows 波动），备选 ipc::Channel 推送 / 复用 WS 桥载 RDP 帧。
- serialport 复核：活跃（4.10.0 2026-08）但 **MPL-2.0** + Linux libudev 动态链接两个打包注意点。
- 文档升版 v1.1，新增 §11 再评估章节；总工作量 36–52 → 33–49 人日。

## v1.2 深度复核（关键结论再次推翻）
- **RDP 也切换到 webview 优先**：确认 `ironrdp-rdcleanpath`（crates.io 独立 crate，MIT/Apache-2.0，450 行 PDU 编解码）可自建 WS 桥；netbird（BSD-3）用 Go 实现同款 RDCleanPath 桥在生产把 IronRDP WASM 桥到任意 RDP 服务器。→ RDP 改「ironrdp-web WASM + 后端 RDCleanPath 桥」，废除自研解码+帧管线+二进制 IPC。
- **WKWebView ws:// 混合内容风险被高估**：Tauri issue #5451 是「HTTPS origin→ws://」才拦截；Tauri macOS origin 是 `http://tauri.localhost`（HTTP），不触发；tauri-plugin-connector 用 ws://127.0.0.1 做 macOS 生产桥实证。R11 高→低。
- 后端最终仅新增：portable-pty / serialport / tokio-tungstenite / ironrdp-rdcleanpath / regex。前端 vendor noVNC core + ironrdp-web WASM。
- 总工作量 33–49 → **24–34 人日**。文档升 v1.2，新增 §12。
- 唯一自研协议代码：Telnet IAC 协商 + RDCleanPath 桥（~400 行，有 netbird 参考）；其余全部复用上游。

## v1.3 跨平台三端复核（架构未变，补平台要点）
- 三端 webview（WebView2/WKWebView/WebKitGTK）的 WASM、WebSocket、canvas 均可用，webview 优先架构三端成立，无架构级阻断。
- 补 4 个平台要点：① CSP `script-src 'wasm-unsafe-eval'`（Tauri 官方要求，否则 ironrdp-web/noVNC wasm 无法加载）；② 剪贴板绕开 WKWebView 禁用 `navigator.clipboard.readText`（项目 #71 已有同款，统一走 tauri-plugin-clipboard-manager）；③ RDP TLS 用宽松验证器+证书透传（TOFU，勿走系统根证书库）；④ WASM 产物 CI 一次构建三端共用。
- 平台基线：Win10 1809+/macOS 12+/Ubuntu 22.04+（WebKitGTK ≥2.38）。总工作量 24–34 → **25–35 人日**。
- 新风险 R15(CSP wasm)、R16(WKWebView 剪贴板)、R17(WebKitGTK WASM 性能/低版本)。文档升 v1.3，新增 §13。
- 沉淀 Skill：`~/.workbuddy/skills/tech-plan-revalidation/`（技术方案多轮证据复核工作流）。

## P0 term 抽象层实施完成（双轨零回归）
- 决策调整：原 P0「SSH 迁移为 SshBackend」降级为**双轨**——SSH 完整留在 `ssh:*`/`SshManager`（零回归最易达成），新 `term` 模块只承载 telnet/serial/local。已记入 plan 文档 §8.1。
- 后端新增 `src-tauri/src/term/{mod.rs,commands.rs}`：`TermIo` trait（read/write/resize/shutdown）+ `spawn_session` 通用读写循环（StreamConverter 编解码 + SessionLogContext + `term:output`/`term:status`）+ `TermManager`（DashMap）+ `TermParams`（serde tag=kind，含 Telnet/Serial/Local 三变体和 LoginScriptStep）。命令 term_open/send/resize/set_encoding/close 已注册，open 暂返回 Unsupported（后端 P1a/P1b/P2 落地）。P0 零新增 Cargo 依赖。
- DB migration 19→20：saved_hosts 加 `kind TEXT DEFAULT 'ssh'` + `params_json TEXT`（幂等列添加）。
- 前端：use-ssh-events.ts 拆 useChannelOutput（通道参数化）+ useSshOutput/useTermOutput；use-ssh-status.ts 同时监听 term:status；Terminal.tsx 按 Session.kind 选通道（两 hook 无条件挂载、null 禁用）；types/ssh.ts 加 TermSessionKind/SavedHostKind。
- 验证：cargo check 零警告；tsc --noEmit 零错误；Terminal.clipboard 5/5 通过；全量 vitest + cargo test 后台跑（结果见下轮）。
- **环境坑（已更新 workbuddy-env-quirks skill）**：npm/pnpm 装包失败根因是 `NODE_OPTIONS=--require=node-language-shim.cjs` 给所有 node 进程注入 broker fs shim（CODEBUDDY_BROKER_DENY），`dangerouslyDisableSandbox` 绕不过；解法 `env -u NODE_OPTIONS <managed-node>/bin/npm install --no-package-lock --cache <workspace>/.npm-cache`，跑 vitest/tsc 同理加前缀。

## P1a 本地终端完成（同日）
- `src-tauri/src/term/local.rs`：LocalPtyIo（portable-pty）实现 TermIo；阻塞 reader 走 OS 线程 + mpsc 通道；slave spawn 后 drop 传 EOF；shell 解析 $SHELL→zsh→bash→sh / pwsh→powershell→cmd（2 个单测）。注意 portable-pty 0.9 的 PtySize 无 cell_width/cell_height 字段。
- term_open Local 分支落地（编码/会话日志复用全局设置）；Dashboard 加「本地终端」按钮（testid new-local-terminal-button）；session-store.addSession 加可选 kind 参数 + label 优先 hostConfig.label；i18n 中英各 3 键。
- 验证：cargo test 250/250、vitest 165/165、tsc 零错误、cargo check 零警告。plan 文档补 §8.2 实施记录。
- **本会话 Edit 工具多次报成功但不落盘**（term/mod.rs logger 改名、local.rs PtySize、HostsDashboard typo 均中招）——关键编辑后必须 grep/sed -n 验证，或直接用 perl -pi 改。

## P1b Telnet 完成（同日）
- `src-tauri/src/term/telnet.rs`：TelnetParser 增量 IAC 状态机（六态、跨分片、255 转义、畸形 resync）+ policy_reply 纯函数协商（DO NAWS/TTYPE/SGA→WILL，DO LINEMODE/ECHO→WONT，WILL SGA/ECHO→DO）+ LoginRunner 登录脚本（regex::bytes 滑动缓冲 expect，parse_escapes \r\n\t\\xNN send 自动补 \r，空 expect 立即发送，60s 超时降级直通，登录期输出 pending 回放）+ TelnetIo（读半边解析内联回写、写 0xFF 翻倍、resize→NAWS、connect 10s 超时）。12 个单测全过。
- term_open Telnet 分支落地；log_context/finish_open 从 Local 分支抽出共用。新依赖 regex = "1"。
- 前端 TelnetConnectModal（主机/端口/编码/动态 expect-send 脚本行）+ Dashboard「Telnet」按钮（testid new-telnet-button）+ i18n 中英各 12 键。
- 验证：cargo test 262/262、vitest 165/165、tsc 零错误。plan §8.3 实施记录。华为/H3C 真机矩阵待 §9.3 手工执行。
- 踩坑：portable-pty/telnet 调试中确认「登录脚本 send 自动补 \r」为契约（mod.rs LoginScriptStep 注释已写明）；测试里 IAC IAC 转义是两个 255 字节，手写测试数据时误写了三个导致 parser 测试失败（parser 正确、测试错）。

## P2 Serial 完成（同日）
- `src-tauri/src/term/serial.rs`：SerialIo（serialport 4, default-features=false 关 udev）——50ms 超时轮询读线程→mpsc；TimedOut/WouldBlock=正常空转，NotFound=拔出结束；dialout/CH340 可操作错误提示；list_ports（USB VID/PID 元数据）+ ensure_hotplug_watcher（2s diff 轮询 → serial:ports-changed，OnceLock 单例）。2 单测。
- serialport 4.10 API 坑：ErrorKind = {NoDevice, InvalidInput, Unknown, Io(io::ErrorKind)}——超时/权限在 Io(...) 里；Box<dyn SerialPort> 的 io::Read 错误已转 io::Error（NoDevice→NotFound）；FlowControl::Software（非 XonXoff）。
- 前端 SerialConnectModal（端口下拉+热插拔刷新+波特率+编码，8N1 固定）+ Dashboard「串口」按钮（Cable 图标，testid new-serial-button）+ i18n 中英各 9 键。serial_list_ports/serial_start_hotplug 已注册。
- 验证：cargo test 264/264、vitest 165/165、tsc 零错误。plan §8.4。
- 环境坑补充：本会话 grep 的 BRE `\|` 交替多次失效（返回 0 误报），多模式搜索用 grep -E 或逐个模式确认。

## 待办（后续会话继续）
- Task #5 (P3)：WS 桥（tokio-tungstenite，127.0.0.1 + 一次性 token，30s 自动关停）+ vendor noVNC + VncCanvas。
- Task #6 (P4)：RDCleanPath 桥（宽松 rustls verifier + 证书链透传）+ ironrdp-web WASM + RdpCanvas。
- SSH 全量 e2e 回归只能在 Linux CI 跑（tauri-driver 不支持 macOS），推 CI 验证 P0 零回归门槛。

## P3 VNC 完成（2026-09-05）

- 后端 bridge：BridgeManager（惰性 127.0.0.1:0 监听、32B 一次性 token、/vnc/<token> 字节直通、30s 空闲看门狗）+ vnc_open/vnc_close 命令；3 个回环集成测试；cargo 267/267。
- 前端：@novnc/novnc + VncCanvas（clipboard-manager 双向剪贴板、卸载即 vnc_close 收尾）+ VncConnectModal + UnifiedTab "vnc" 变体（id=token, wsUrl）+ AppShell render-all 分支 + ScreenShare/MonitorUp 图标 + i18n 中英 9 键 + novnc.d.ts。
- 验证：cargo 267/267、vitest 165/165、tsc 零错误；方案文档新增 §8.5。
- 坑（重申）：Edit 工具对 HostsDashboard/i18n 的多处编辑报成功但未落盘（本轮 4 次），全部改用 python3 脚本修复并 grep 验证；grep BRE \| 假阴性再次出现，坚持用 grep -E；heredoc 内含 \n 转义会被破坏，多行 python 一律写成临时 .py 文件再执行。
- 下一步：Task #6 P4 RDP（RDCleanPath 同监听器 /rdp/<token>、ironrdp-web WASM、RdpCanvas、CSP wasm-unsafe-eval）。

## P4 RDP 完成（2026-09-05）—— 全部 6 个任务收官

- 调研结论：ironrdp-web 无 npm 包；官方 @devolutions/iron-remote-desktop 0.11.0（Web Component，`module` 属性注入 Backend，`ready` 事件给 irgUserInteraction，connect(config)+run() 终止信号）+ @devolutions/iron-remote-desktop-rdp 0.7.0（WASM base64 内嵌 JS，init("warn") 加载）。代理契约以 ironrdp-web connect_rdcleanpath 源码 + netbird client/wasm/internal/rdp 双重核实：代理自己做上游 TLS（宽松验证器、仅 TLS1.2）截证书链回 new_response，之后 WS↔TLS 隧道对拷，客户端 CredSSP 在 upgraded 连接上跑。
- 后端：remote/rdp.rs（RDCleanPath 代理）+ Route::Rdp + rd_open/rd_close；rustls 0.23.37 已无 dangerous_configuration feature（直接用 dangerous() 方法）；测试假 RDP 服务器用 rcgen 0.14（API 是 generate_simple_self_signed，字段 cert/signing_key）+ rustls server 也要显式 ring provider。cargo 269/269。
- 前端：RdpCanvas + RdpConnectModal + UnifiedTab rdp 变体 + AppShell 分支 + MonitorPlay 图标 + i18n 中英 11 键。坑：noVNC 1.7 exports 只暴露根路径，@novnc/novnc/core/rfb.js 深导入 vite build 失败，改 import("@novnc/novnc")。vite build 通过（RDP WASM 6MB 动态分包）。
- 终态验证：cargo 269/269、vitest 165/165、tsc 零错误、vite build 成功；方案文档 §8.1–§8.6 实施记录齐备。
- 待办（后续）：Linux CI SSH 全量 e2e 回归（tauri-driver）；真机矩阵 §9.3（华为/H3C telnet、USB 串口、真实 VNC、Windows NLA RDP）。

## P5 主机保存/复连闭环完成（2026-09-05）

- 打通 saved_hosts.kind/params_json 读写（P0 只做了 migration，无读写路径）：SavedHost 结构 + serde default、upsert/list/get SQL、kind NOT NULL 显式 NULL 不取列默认值 → 保存时 unwrap_or("ssh")（坑，曾致 24 个 db 测试 ConstraintViolation）。
- 前端 lib/protocol-hosts.ts（build/persist/isSshHost）；四个快速连接弹窗连接成功自动保存主机卡（params_json 与 term_open 参数一致，复连零翻译）；connectToHost 顶部 isSshHost 分流复连（telnet/serial→term_open、vnc→vnc_open、rdp→预填 modal 重输密码，凭据永不落盘）；非 SSH 卡显示协议徽标、隐藏 SFTP 入口。
- 验证：cargo 270/270（+kind roundtrip 测试）、vitest 165/165、tsc 零错误、vite build 通过；方案文档 §8.7。

## CI 预检收尾（2026-09-05）

- 关键修复：本次多功能扩展用 npm --no-package-lock 装的 @novnc/@devolutions 未进 pnpm-lock.yaml，CI frozen-lockfile 必挂 → env -u NODE_OPTIONS pnpm install --no-frozen-lockfile 更新（pnpm 接管 node_modules，tsc/vitest/build 复验通过）。
- 清零 4 个 clippy 警告（CI 是 -D warnings，此前从未本地跑过 clippy）：rdp.rs 范围包含写法、telnet.rs while-let + doc 空行、bridge.rs accept_hdr_async 回调显式标注 Result<Response, ErrorResponse> + #[allow(clippy::result_large_err)]（tungstenite ErrorResponse 本身就是 136B 的 Response 别名，box 不划算）。
- cargo fmt --all 应用（P4/P5 新代码未 fmt 过）。
- 坑（新）：python -c "..." 内注释含反引号会被 shell 命令替换吃掉内容 —— 多行 python 一律写临时 .py 文件（旧坑再现变体）。
- CI 预检终态：pnpm build ✓ / pnpm test ✓ / cargo fmt --check ✓ / clippy 0 警告 ✓ / cargo test 270/270 ✓。e2e 为 Docker Linux-only，push 后自动跑。
