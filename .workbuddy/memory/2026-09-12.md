# 2026-09-12

## anySSH 正式发布前——实机人工验证（按用户「按顺序做，需要确认通知我」推进）

上一轮的 10 项实机验证开始逐项落地。**本轮新增的全部结论都有命令输出或文件哈希支撑，无一条凭推测判定。**

### 已完成：第 9 项（升级 + 回滚）整项闭环，外加 macOS 安装

设备/环境：macOS arm64 实机、jason 的真实安装（`$HOME/Library/Application Support/com.jincaiw.anyssh`，schema 20、3 分组、11 设置）。执行前已完整备份数据目录到 `.workbuddy/backups/anyssh-data-20260912-134059`。

| 项 | 结果 | 关键证据 |
|---|---|---|
| macOS 安装 + 首启（第 6 项 macOS 部分） | PASS | `ditto` 安装 0.15.0，终端启动日志 `INFO anyssh_lib::db: database initialised` |
| Gatekeeper（R-8 取证） | 复现 | 手工打 quarantine → `spctl` rejected；`gh`/`curl` 下的 dmg **不带** quarantine ⇒ 命令行路径天然测不到 |
| 升级后数据保留 | PASS | 逐表行数 + 3 分组逐字段 + 原 10 条设置逐字一致 |
| **回滚本身** | PASS | 0.14.46 能读 0.15.0 的库；**对库零写入**（mtime 不变、`-wal` 0 字节）；快照 `diff` 完全一致 |
| **schema 闸门** | PASS（拒绝正确） | 置 `schema_version=21` → **exit 134**；stderr 打印 `schema v21 … understands up to v20`；数据未损 |
| **应用内升级端到端** | PASS | 0.14.46 + `autoUpdate=true` → **20 秒内**自升级为 0.15.0；装出的 bundle 与官方 dmg **逐字节一致**（可执行 / Info.plist / 全 bundle 递归哈希三项全等）⇒ 同时证明 minisign 校验通过 |
| `autoUpdate` 语义 | PASS | `false`：端点可达且通告 0.15.0、启动确实执行检查，但 PID/版本/包 mtime 三项不变；jason 肉眼确认**弹出了「发现新版本 0.15.0」**。`true`：静默升级 |

### 本轮最有复用价值的三个方法论点

1. **「什么都没发生」不是充分证据。** 验 `autoUpdate=false` 时，若更新检查因网络失败根本没跑，观测结果与「标志生效」**完全一样**。必须做**双向对照**（置 `true` 确认真的会自升级）才能封住这个推理缺口。**后续任何「某开关抑制了某行为」的验证都适用。**
2. **「关掉自动更新」关的是静默安装，不是联网。** `checkOnStartup` 在 `autoUpdate=false` 时仍会请求 `latest.json`，只是改为弹窗。凡涉及「关掉 X 就没有出站」的推断都必须抓包证实，不能凭开关推断。
3. **旧版回滚测不出 schema 闸门。** 闸门是 v0.15.0 才引入的，且 0.14.46 与 0.15.0 **同为 schema 20** ⇒ 回滚**零迁移**。原手册「旧版应提示库版本更高」是**事实错误**，已修正为 A（回滚本身）/ B（人为造 `schema_version=21`）两段。

### 新缺陷：R-9（有实测证据的用户可见问题）

schema 闸门**拒绝得正确**，但呈现是 panic：`lib.rs` 的 `.setup()` 返回 `Err` → Tauri 内部 panic → 末端 `.run(...).expect(...)` ⇒ **exit 134 / SIGABRT**，报错只走 stderr。而日志又不落盘（R-7）⇒ **Finder 双击启动的用户拿不到窗口、对话框或日志，现象是「点了没反应」**。这是 R-7 从理论隐患变成实测危害的实例，建议发布前修（加 panic hook + 原生错误对话框）。

### 环境坑（本会话又踩到 / 新发现）

- **BSD `grep` 的 `\|` 交替静默失效，本次又踩一次**：`grep -n "^## 8\|^### 7.4\|…"` 返回**空**，导致我一度以为第 8 节标题丢失是别的原因。该坑 2026-09-10 已记录**仍复发** ⇒ 遇到多分支 pattern 一律直接用 Grep 工具（rg），不要用 `grep`。
- **`log` 被 zsh 内建同名函数遮蔽**：`log show …` 报 `failed to load module zsh/watch`。要用 `/usr/bin/log`。
- **`launchctl setenv` 在沙箱不可用**（`Not privileged to set domain environment`）⇒ 用 `open` 启动的 GUI 应用**无法注入环境变量**。想在保留环境变量的同时让进程活过工具调用结束，本会话**没找到办法**；`nohup … &` 仍会在 Bash 调用结束时被回收（表现为「进程莫名退出」，**不是崩溃**——判据：日志无 panic、且用 `open` 重启后跨多次独立调用稳定存活）。
- **`open` 支持 `--stdout/--stderr`**，但 GUI 应用的 stdout 不进统一日志（tracing 只写 stdout）⇒ `log show --predicate 'process == "anyssh"'` 恒为空，不要指望它。
- **`osascript` 读窗口名、`screencapture` 截图均被 TCC 拒绝**（AppleEvent `-10004`、`could not create image from display`）⇒ **本环境无法取得任何界面视觉证据**，必须请 jason 人眼确认（这次正是靠他确认了更新弹窗才封住证据链）。
- `sqlite3` 可直接改库（应用退出后），比走 UI 快得多；但要在报告里写清楚「这行是验证时写入的、原库没有」。

### 文档产出

- 报告新增 **§12.1 已执行的实机验证**（V1–V7 七项，含方法/结果/证据三列 + 明确列出「仍未验证」的项）；新增 **R-9**；§1/§7/§9/§10/§13 同步更新；修正 §12 第 9 项原本**错误的期望值**。
- 手册新增 **§7.4 执行记录**（含两条必须记住的补充说明）；§7 事实 1 补「开关语义」；「本机现状」表更新为已装 0.15.0 并**注明库中 `app_auto_update='false'` 是验证时写入的、原库无此行**；汇总表 13 项勾 2 项并标注剩余。

### 未完成 / 下一步

- 手册 §7.3 更新器兜底（断网 / 下载中断 / 跳过此版本）未验，需界面操作 + 断网。
- 第 2 项 VNC 需先开 macOS「屏幕共享」（改系统设置，**须先取得 jason 同意**）。
- 第 8 项 4 小时长跑（采样只能用 `pgrep`+`lsof`，`ps` 被沙箱禁）、第 10 项遥测抓包（需 root + 对照组）。
- 第 1/3/4/5/7 项需外部设备（Windows RDP 主机、telnet 网元、USB 串口、堡垒机、高 DPI 环境）。
- **未授权**：打新 tag / 发布新 Release。

---

## 续：E2E 编排被上游打断——查出并修复（`ba5b8d1`）

### 起因：查文档提交为什么是红的

推完文档后核对 CI，发现**两条更早的纯文档提交也是红的**（此前只记到 25b2a92、2c31fd2）。查下去发现失败根本不是一类东西：

| 提交 | 现象 | 性质 |
|---|---|---|
| `25b2a92` | 1 shard，`19-sftp-refresh` | 环境性 |
| `2a63485` | 2 shard，`38-edit-auth-type` + `04-connect-key`（publickey 被拒） | 环境性 + **未解释** |
| `7b57755`、`7e8d3b8` | **4 shard 全挂，0 个 spec 运行** | **确定性编排故障** |

### A 类：MinIO 镜像从 Docker Hub 被移除（已修）

`make e2e-pull` 每次都挂在 `pull access denied for minio/minio`。**区分「仓库被删」与「限流」是关键**——两者都是 `docker pull` 失败，含义完全不同：

- 取 registry token 后解 JWT 的 `access` 声明：`granted_access=[]` + manifest `401` ⇒ **仓库未公开/已删**（限流是 429 / `toomanyrequests`）。
- 命名空间列表交叉验证：`minio` 命名空间下 20 个公开仓库里**已无 `minio`、`mc`**。
- 搜索确认：MinIO 于 **2025-10-23** 停止发布社区版镜像（Docker Hub 与 Quay 都曾下线）。
- `quay.io/minio/{minio,mc}` **仍返回 200**（冻存的最后一版）⇒ 修复只需换 registry 前缀。

**必须带阳性对照**：同一份探测代码先打 `library/alpine`、`library/busybox` 得 200，才敢信那两个 401。差点又栽在"探测器自己坏了"上。

用户选择换 quay.io（推荐项）。Chainguard 的免费镜像（`cgr.dev/chainguard/{minio,minio-client}`）也 200，但**是 distroless、没有 shell**，`minio-seed` 的 `entrypoint: sh -c` 会直接跑不起来 ⇒ 不是"改个前缀"就能换，已如实告知。

### B 类：环境性抖动（未修，也不该靠改测试修）

`ba5b8d1` 同一二进制跑三次：attempt 1 三红 → attempt 2 一红 → attempt 3 **全绿（75/75）**。**失败 spec 每次都在换**。

**新增一条比「重跑才绿」更硬的论证**：分片是**确定性 round-robin**（`wdio.conf.ts` 排序后 `i % 4`），所以每个 shard 的 spec 集合**固定**。把近期所有失败过的 spec 映射回 shard，结果是**分散在全部 4 个 shard、且都落在预期 shard 里**——若是某个 spec/shard 有真实缺陷，失败应当**集中且每轮复现**。这条比"换个 spec 失败"更难反驳。

根因候选（有日志支撑）：容器内 `libEGL DRI3 error` ⇒ WebKitGTK 软渲染；**runner 镜像没装 mesa 软件光栅化后端、也没设 `WEBKIT_DISABLE_COMPOSITING_MODE`/`LIBGL_ALWAYS_SOFTWARE`**，所以回退是听天由命的。**根治方向是让渲染确定化，不是调大等待预算**——已写进报告作为未做项。

### C 类：未解释，且推翻了此前的错误判断

`04-connect-key` 报 `publickey ssh-ed25519 rejected`。**决定性证据**：同 shard 的 entrypoint 就绪门在 **15:27:47** 打印 `sshd-key accepts the test key`，失败在 **15:29:01**——**74 秒后**；且 spec 与就绪门用的 host/port/user/key **完全相同**（已核 `USER_NAME`、`PUBLIC_KEY_FILE` 与 spec 默认值）。

⇒ **不是就绪竞态**。报告里"该类已由 `0fc493d` 修复"的说法**不成立**（就绪门当时是通过的）。这是本项目**第一个被自己的实测推翻的既往结论**——已按证据改写，并保持"不判定为通过"。

### 另一处被推翻的论断

报告 §10 原写「纯 Markdown 的报告修订**不重跑 CI**」——**错的**。`ci.yml` 对 `main` 每次 push 都无条件运行（三条纯文档提交的 run 都跑了且都红）。已改写，并补上后果：**那几条提交无法用"重跑旧 run"回头验证**（A 类是编排层面的，重跑同样提交仍卡在镜像拉取），等价证据改为在**源码完全相同**的 `ba5b8d1` 上取到的全绿。

### 方法论点（可复用）

1. **「0 个 spec 运行」与「spec 失败」是两种病**：前者是编排/基础设施，重跑永远不会好；后者才可能是抖动。看到 4 个 shard 同时挂，先怀疑拉取/启动阶段，不要直接归入"抖动"。
2. **确定性分片可以让归因变硬**：把失败 spec 映射回 shard，检查"是否集中"。分散 + 每轮换点 ⇒ 环境；集中 + 复现 ⇒ 真缺陷。
3. **推翻自己的旧结论要留痕**：把"此前判断不成立"写进报告并给证据，比悄悄改掉更有价值。
4. 探测外部资源（registry/端点）**一律先跑阳性对照**，否则分不清"目标坏了"和"探测器坏了"。

### 状态

`ba5b8d1` 已推送并 CI 全绿（attempt 3，75/75 spec）。报告 §1/§3.4/§10/§11/§12.1/§13 与 §13 结论已按上述三类失败重写。

